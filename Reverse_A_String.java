package Incresol;

import java.util.List;
import java.util.stream.IntStream;

public class Reverse_A_String {
public static void main(String[] args) {
	String s="Bandaru";
//	String reversed = IntStream.rangeClosed(1, s.length())
//					.mapToObj(i->s.charAt(s.length()-i));
////					.collect(String);
	 
		
}
}









/*
 * As characters flow:
 * 
 * Convert int → char
 * 
 * Add each character at the start of the result This automatically builds a
 * reversed string
 */